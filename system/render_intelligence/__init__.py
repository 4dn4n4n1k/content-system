"""Render Intelligence: turns timing + shot list + editorial reports into
render_plan.json — the decision record the assembler executes. Planning
only; no video is rendered here. See planner.build_plan / ensure_plan."""
